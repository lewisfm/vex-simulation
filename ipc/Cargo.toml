[package]
name = "roboscope-ipc"
version = "0.1.0"
edition = "2024"

[dependencies]
bitflags = "2.11.1"
derive_more = { version = "2.1.1", features = ["try_into", "from", "debug"] }
iceoryx2 = "0.8.1"
thiserror = "2.0.18"
vex-sdk = "0.28.0"

[features]
default = ["thread-safe"]
thread-safe = []
